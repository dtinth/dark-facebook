
button:
  selector: ".uiButton"
  children:
    text:
      selector:
        - ".uiButtonText"    # for most buttons
        - "& input"          # ex. like page, respond to friend request

button(mar2014):
  selector: "._4jy0, a._4jy0, ._59pe:focus, ._59pe:hover"

button(share-dialog):
  info: |
    Buttons found in share dialog...
  found: Share Dialog
  selector: "._42fu._42fu._42fu"   # repeat the selector so it wins the link colors
  children:
    primary:
      info: This is a primary button. Usually it is in blue.
      selector: "&._42g-"